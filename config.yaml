bot:
  name: "Hecate"
  debug: true
  log: test.log
  networks:
    token: "figs"
    ssl:
      cert: "cert.pem"
      key: "key.pem"
    external:
      port: 80
      host: "http://localhost"
    internal:
      port: 8443
      host: "http://localhost"
